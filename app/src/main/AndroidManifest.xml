<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mywhatsappclone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyWhatsappClone">
        <activity android:name=".FriendsProfileActivity"></activity>
        <activity android:name=".ChatScreenForGroup" />
        <activity android:name=".ChatScreen" />
        <activity android:name=".Authentication.OtpManage" />
        <activity android:name=".Authentication.PhoneNumberInputPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>